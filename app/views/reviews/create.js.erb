window.location.reload()

// $('#new_review').remove();
// $('#add_review_link').show();
// $('#review-list').prepend('<%= escape_javascript(render partial: "reviews/review", locals: {review: @review}) %>');
