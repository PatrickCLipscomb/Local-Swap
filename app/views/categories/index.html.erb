<h1>Category Page</h1>
<hr>
<br>
<ul class="list-group well">
  <%= content_tag_for(:li, @categories, class: "list-group-item") do |category| %>
<%= link_to category.name, category_path(category) %> <%= link_to "Delete Category", category_path(category), remote: true, :method => 'delete', :class => "btn btn-danger btn-sm pull-right" %>
  <% end %>
</ul>
<hr>
<%= link_to "Add a category", new_category_path, :class => "btn btn-success btn-lg"%>
